# Skill: /dev

Start the RYUZUNO development server.

## Instructions

1. Run `npm run dev` to start Vite dev server on port 8080
2. Confirm the server is running and report the URL (http://localhost:8080)
3. If port 8080 is already in use, inform the user and suggest killing the process or using a different port
