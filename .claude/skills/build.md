# Skill: /build

Build the RYUZUNO project and check for errors.

## Instructions

1. Run `npm run build` to create a production build
2. If the build fails, analyze the TypeScript or build errors
3. Fix any errors found and re-run the build
4. Report the build result (success/failure) and output size
